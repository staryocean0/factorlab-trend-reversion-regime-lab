# 本地只交付一天原始记录，云端核验，不做策略研究

目标仓库：`staryocean0/factorlab-trend-reversion-regime-lab`。
先保护未提交改动，安全 fetch 并 fast-forward 最新 main，不强推、不覆盖远端工作。

上次 `data/etf_upstream_evidence_v1/` 的35个交付文件已校验。时间合同与20个固定样例已取得，不重传这批材料、不重传五年分钟CSV、不重做份额合并审计，也不重开 R1_A。

## 本次唯一日期：2025-12-01，完整来源交易日

这个日期来自已提交盘点中两只ETF同时有报价与成交的那一天，选日依据是同窗可得性，不是价差、波动或收益。这是数据工程样本，不是代表性验证集；没有授权更换日期、扩展全年、读取2026或自动开始回测。

只使用已经存在、合法可访问的本地 DataHub 湖。交付以下六个逻辑文件（CSV或Parquet均可），按品种和源交易日过滤，但不按分钟、价差、成交量或价格有效性筛行：

| 文件 | 品种 | 来源产品 |
|---|---|---|
| ETF报价 | 512100.SH | fund_cb_l2_quote_change_cn_a_3s_baidu_shidang_20250901_20260828_v1_20260902 |
| ETF报价 | 588000.SH | 同上 |
| ETF成交 | 512100.SH | fund_cb_tick_trades_cn_a_baidu_shidang_20250901_20260828_v1_20260902 |
| ETF成交 | 588000.SH | 同上 |
| 指数原始3s观察 | 000852.SH | market_index_baidu_3s_20000714_20260821_cffex_underlyings_alias_repaired_v4_20260823 |
| 指数原始3s观察 | 000688.SH | 同上 |

盘点宣称报价行数分别为4823/4975，成交行数为10376/93639；这些仅用于核对上次盘点，不是本次必须凑齐的门槛。若实际文件或版本不同，请如实报告差异及原因，不删行凑数。两个指数当天是否有数据仍需实际检查，不能用整个产品的时间范围证明这一天存在。

## 保留原字段和表示

报价保留原始十档买卖价格及量、`time_raw`、`market_observed_at`、原行号、顺序/消息编号、状态、available_at/ingested_at/source_kind/dataset_version，以及原文件中其他字段。不要只截买一卖一，不把变化记录自行变成完整快照。

成交保留原始时间、价格、数量、成交额、方向、成交号/买卖委托号/序号及状态；没有的字段列明缺失，不编造。指数保留 `observation_datetime`、`observation_time`、price、amount、row_index、timestamp_mode、source_kind及所有原字段。

价格为x10000整数就原样保留。不得把旧1m的Z解码规则直接套到新的L2/tick/指数产品上。不得平移时钟去追求相关性，不去重，不填价，不把quantity=0改成成交，不把入库时刻重命名为交易所发布时间。

## 同包附必要说明，而不是仅交报告

1. 六个逻辑文件对应的源产品版本、源分区相对路径、源文件SHA256、schema；导出脚本、过滤条件、行数、字节数、导出文件SHA256、源时间字段首末值。
2. 本地已有的产品字典、导入/解码相关代码摘录及版本/hash：每个时间字段属于原观察、交易所事件、供应商发布还是入库时间；时区、单位、时间精度与3s量化/采样/去重规则。
3. 报价到底是完整十档快照、全量状态变化采样，还是必须依赖前一条才能还原的增量消息；同一时刻多条记录如何排序、是否存在重置/更正；若需要基准快照，请交当日对应的初始化状态。
4. 价格/数量/金额的实际量纲依据。字段名不是独立证明；没有正式依据的写UNKNOWN，不先换算。
5. 同日已知停牌、公司行动、异常状态的来源资料（本地已有则附；缺少则明确）。不要求为了交一天工程样本穷尽两只ETF五年的公告。不要用旧588000空表证明当天必然无事件。
6. 有 IOPV 原始字段/产品时仅报告现有可得性与定义，不为本次另行采购。没有IOPV不妨碍交上述六类记录，但本次不能据此声称算出了真实NAV折溢价。

零量样例中有 `512100.SH 2025-12-01 13:26:00+08:00`。本次保留整天记录即可；云端将检查该时间附近是否实际存在可用的成交/报价，不能因为日期相同先宣称已经找到了这根bar的逐笔真值。

## 权限与交付

新目录建议 `data/etf_microstructure_sample_20251201_v1/`。公开再分发获准的材料才能提交公共Git；研究使用权限不等于公开权限。只能私有交付的材料在允许范围内打包为附件，不能公开则不强行上传。无权限时只报告精确限制，不购买、不借凭据。

不能在GitHub或附件中包含账号、token、cookie、含凭据的下载链接。不改旧包、旧manifest、旧行情、信号、配对、收益和阈值。

公开交付后从远端指定提交做干净检出，确认实际六类文件可读且hash/行数一致，不依赖本机路径；私有附件则干净解压核验。不要仅推清单、LFS指针或符号链接。缺某类文件时交齐其余已有文件，明确缺口，不用1m/日线替代。

完成时报告 commit SHA（或私有附件路径/hash）、实际文件清单、各自行数与源版本、尚未知字段、未取得文件。本地到此结束；云端负责后续验收与判断。本任务没有收益回测、领先滞后拟合、阈值选择、实盘或购买授权。
