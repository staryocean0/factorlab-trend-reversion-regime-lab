# Broad reversal / mean-reversion Stage-1 discovery round closeout — 2026-09-08

Program identity: `broad_reversal_mean_reversion_discovery_program_v1`

Decision:

`STAGE1_DIRECTION_DISCOVERY_ROUND1_CLOSE_NEW_LANE_GENERATION_PAUSED_PRIORITIZE_R1_SPECIALIST`

## Why this closeout is needed

The broad repo is a direction finder, not an unlimited indicator search. It has now given comparable shallow budgets to multiple structurally distinct mechanisms and has accumulated enough negative/positive evidence to rank directions.

Continuing to invent R8/R9/... on the same consumed 2015–2022 window without a new scientific reset would increase multiplicity and turn broad discovery into post-hoc feature fishing.

Therefore automatic new-lane generation is paused after R7.

## Evidence map

### Priority A — R1 cross-scale pullback / parent integrity

**Strongest discovery.**

Fine S1-inside-S2 stability:

- n = 763;
- severity-only Brier `0.18090`;
- parent+severity Brier `0.17229`;
- improvement in 2020/2021/2022.

Coarse S2-inside-S3 stability:

- n = 291;
- severity-only Brier `0.16285`;
- parent+severity Brier `0.15601`;
- improvement in 2020/2022, small deterioration in 2021.

Interpretation: parent structural context adds economically interpretable probability information beyond counter-move severity.

Action: **move to a dedicated specialist identity** before any PnL work.

### Priority B — R5-C event density

**Progressed, but weak.**

- S1 Brier improvement ~`0.000745` with 3/3 annual direction;
- S2 improvement ~`0.000093` with 2/3 annual direction;
- large sample supply, simple state variable;
- effect much smaller than R1.

Action: keep as secondary specialist candidate; do not consume broad-program reserve or displace R1 priority.

### R2 — range-boundary reversion

**Hold, not promoted.**

Fine scale unstable; coarse combined model mildly positive but parent range state alone did not isolate the mechanism.

### R3 — regime-conditioned residual

**Closed.**

The frozen low-capacity conditional residual did not show stable next-session mean reversion or meaningful advantage over unconditional deviation.

### R4 — relative-value / V21 P2

**Closed for the tested successor family.**

DEV annual sample-sufficiency gate failed; Audit A/B remain sealed.

### R5-A path efficiency

**Closed as price-path predictor.**

The statistic itself strongly reverts, but incremental price reversal/continuation information is not cross-scale stable.

### R5-B volatility displacement

**Closed.** Cross-scale price-path relation unstable.

### R6 fixed multi-scale amplitude ratios

**Closed.** Neither fixed 4/16/64 amplitude concentration score improves the severity baseline at both S1/S2.

### R7 directional path-energy asymmetry

**Closed.** S1 is consistently positive but S2 is negative, so the frozen cross-scale gate fails.

## Important cross-lane scientific finding

Several statistical-state objects show strong reversion toward their own rolling normal region while failing to provide stable price-path mean-reversion information:

- R5-A path efficiency;
- R6 amplitude-state scores;
- R7 directional-energy asymmetry.

Therefore the program must keep the distinction:

> **reversion of a statistic is not evidence of price mean reversion.**

A statistical property is useful only if it adds stable causal information about the price-path recovery/extension outcome or improves parent-state classification in a separately preregistered objective.

## Multiplicity / research-budget decision

Automatic broad-lane creation is paused now.

Do not define another shallow indicator/state lane on the same 2015–2022 window unless one of the following occurs:

1. the user explicitly resets the broad discovery program with a new mechanism class;
2. materially new data/source becomes available and its role is frozen results-blind;
3. a new literature/theory-driven mechanism is defined independently of these Stage-1 outcomes and receives an explicit program review before empirical execution.

This prevents an open-ended sequence of weak tests from diluting the strongest discovery.

## Preserve the unopened reserve

`2023-01-01 .. 2025-12-31` remains unopened for all broad Stage-1 mechanisms.

That reserve is not scientifically fresh across the user's overall research history, but it remains a clean within-program mechanism holdout. Its value increases by **not** consuming it during broad lane invention.

## Next research budget

### First priority

Execute the R1 specialist handoff under a dedicated identity:

`rmr_cross_scale_pullback_parent_integrity_v2`

Before opening 2023–2025, the specialist must freeze:

- exact parent-integrity representation;
- exact event/scale role;
- candidate budget;
- held-forward success/failure gates;
- reserve usage;
- no-PnL mechanism objective.

### Second priority

R5-C `rmr_event_density_state_reversal_v2` remains queued behind R1. It should receive a stricter event-geometry baseline before any holdout opening.

## What is not authorized

- broad R8/R9 indicator generation by default;
- opening 2023–2025 to choose a new lane;
- PnL optimization in the broad repo;
- reopening R2/R3/R4/R5-A/R5-B/R6/R7 under the same identities;
- deep-model rescue of failed shallow mechanisms;
- production authority.

Production authority remains `false`.
