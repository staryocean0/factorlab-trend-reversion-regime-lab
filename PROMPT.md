# 给接手外部研究AI的提示词

你接手一个全新的FactorLab研究课题：**基于K线状态、波动率、成交额/量、序列依赖和可获得的市场微观结构，研究趋势跟随与趋势反转/均值回归的条件适配。** 主要对象是中证1000与科创50指数。不是优化旧科创50滤波策略，也不是把其他仓库的模型、参数或结果移植过来。

## 先确认工具和材料

1. 阅读`AGENTS.md`、`RESEARCH_FRAMEWORK.md`、`docs/DATA.md`、`docs/INFRASTRUCTURE.md`、`docs/RESEARCH_GOVERNANCE.md`。
2. 本仓是public，所以可以读取；能否新建/修改文件取决于你实际有无GitHub写权限及工具。不要承诺“联网即可修改”。有授权时使用独立分支/PR；没有时输出patch或文件包，由用户转交。不要索取或打印密钥。
3. 论文全文由用户单独提供ZIP；按`literature/references.json`辨认版本。没有读到全文就明确说未读。禁止将这些PDF上传本仓或公开release/issue附件。
4. 安装环境并运行`python scripts/validate_seed.py`和`python -m pytest -q`，记录真实命令和结果。若只有网页阅读而没有执行工具，不得声称已运行测试或回测。
5. 原框架包含外部会话的`sandbox:`链接和合成示例指标，本仓没有这些附件；不把它们当成本项目已经实现的策略或实际结果。

## 研究目的

不要先假设某个指数永远适合趋势、另一个永远适合均值回归。区分：

- 同一资产、不同物理期限可能同时呈现持续和反持续；
- 高波动水平、波动扩张、孤立跳变、少量bar集中位移与真实趋势不是同一个概念；
- 负自相关可能来自报价离散化/买卖价反跳，不必然是可盈利反转；
- 同样成交额放大，可能伴随信息更新或流动性冲击，仅OHLC和成交额不能自动识别因果来源；
- 市场状态测量、未来状态预测、方向信号、策略适配和真实执行分属不同层次。

用户框架中的分数、阈值和持有周期是待检验假设，不是本项目锁定参数，也不是文献证明的自然常数。文献里的其他市场结论不能直接当成两指数实证。

## 首轮工作顺序

### A. 先建策略级基础设施，不先找赢家

本仓已有通用项目基础设施，但尚无该新策略的白皮书、信号代码或结果。由你建立其五位一体结构：研究文档、数学白皮书、代码、测试、可执行工作流。新文件建议放`docs/research/`、`src/regime_lab/`、`tests/`和`experiments/`，不要改造或注册本地生产策略。

### B. 做数据能力与机制可行性审计

使用`regime_lab.market_data.load_market_data`读取包内固定数据。先检查共同交易日/会话、原生采样密度、同秒多行、修复/缺失标记、价格与成交额单位、时区和观察时点。指数volume是null；不得将amount冒充股数或换手率。不存在OFI、depth、撤单、逐笔方向、IV或新闻输入时应声明缺口，而不是填0制造假特征。

不要在FactorLab本地制造新的墙钟OHLC产品。现有3秒只能作为真实观测序列；单边价格网格必须明确是研究测量，不冒充DataHub新bar。需要其他合法频率时通过交接说明请求最小DataHub出口。

### C. 在看模型胜负前落协议

使用`templates/PROTOCOL.md`，明确：研究假说、可否证机制、数据角色、共同样本、物理horizon和回看、特征可得时点、未来标签、重叠purge、训练/校准/检验界限、候选数量、费用情景及评价指标。不得随机打散时间或使用centered/future-smoothed特征。

当前数据在项目其他研究中已被消费，新仓库不使它变fresh OOS。可以用于开发与标明身份的历史重复检验；若未来要求真正未见样本，需要另行取得并冻结。2026不在本包，不主动打开旧仓库的2026数据。

### D. 分开验证状态与策略适配

先检验持续/反持续/过渡状态的稳定性、可预测性与不同尺度的条件关系，再评估简单、透明的趋势与均值回归控制策略。不要从最赚钱的回测反向定义“正确状态”，也不要把策略收益泄漏进同一时点feature。

早期prototype可以使用合成单调、往返、跳变、缺口等反例测试；真实历史结论必须记录完整搜索次数、失败案例及不确定性。若原文方法不合适，可提出更好的数学设计，但必须解释金融目标、输入条件和统计代价，不能只是扩大调参网格。

重点报告平均单笔质量与风险调整表现，并兼顾覆盖、换手、成本、尾部和回撤；不以总收益唯一择优。指数点位不是直接可成交资产；任何指数方向模拟须这样标注。真实ETF/期货/期权执行需要另立载体合同，当前不授实盘或期权研究权限。

## 交付与协作

- 每次成果写`templates/RESULT_CARD.md`对应的结果卡和实验台账，包含假说、数据/代码hash、已执行命令、完整结果、失败项、限制和下一步。
- 既有项目源码仅作基础设施；其中菜单/默认值不代表新策略参数已选定，年度聚合函数不是逐时点因果特征。
- 不要把代码写完、测试通过或回测盈利自动升级为策略有效、fresh OOS或生产准入。
- 云端能直接执行的就执行；缺本地数据/能力时写`docs/cloud_local_communication.md`的最小请求，不默认启动GitHub Actions或大规模云任务。
- 本地仍是存储核心。交付commit、文件清单和运行证据，供本地控制器回迁验收。不要改另外四个主题仓库。

**第一条回复应说明材料/执行/写权限是否具备，并提出首轮协议与基础设施计划；不要虚构已有新策略结果。**
