name: r1b-mo-data-admission-gate

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'research/r1b_mo_data_admission/**'
      - 'research/r1b_mo_pre_execution/**'
      - 'research/r1b_mo_outcome/**'
      - 'research/r1b_mo_backspread/**'
      - 'tests/test_r1b_mo_*.py'
      - 'docs/governance/R1B_MO_*.json'
      - 'pyproject.toml'
      - '.github/workflows/r1b-mo-data-admission-gate.yml'

permissions:
  contents: read

jobs:
  admission-gate-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            pyproject.toml
            src
            shared
            research
            tests
            docs/governance
          sparse-checkout-cone-mode: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install package
        run: python -m pip install -e .
      - name: Run MO admission and closed-payoff reproducibility tests
        run: python -m pytest -q tests/test_r1b_mo_*.py
