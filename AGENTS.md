# Trend / Reversion Regime Research Seed

Read `PROMPT.md`, `RESEARCH_FRAMEWORK.md`, `docs/DATA.md`, and
`docs/RESEARCH_GOVERNANCE.md` before research. This is a new independent project,
not a continuation of a previous strategy. No old strategy, parameter winner,
model, or backtest result has been supplied.

- Subjects: CSI1000 000852.SH and STAR50 000688.SH; 3s observations and prebuilt 1m/5m bars.
- Local FactorLab/DataHub remain the storage authority. Cloud is a research venue.
- 2026 and unrelated markets are excluded. Supplied history is already consumed
  material, not fresh OOS merely because this strategy is new.
- PDFs are supplied privately by the owner. Never add PDF/archive attachments
  containing literature to this public repository or to its releases/issues.
- Public visibility is read access, not write authority. Use an authorized
  GitHub integration and branch/PR, or deliver a patch if unavailable. Never
  request tokens in chat or store credentials.
- Run `python scripts/validate_seed.py` and `python -m pytest -q` before handoff.
- No production registration, live orders, or mutation of other repositories.
- Work directly in the cloud when possible; use `docs/cloud_local_communication.md`
  for minimal missing inputs. Do not default to Actions or fake execution evidence.

Preregister data roles, horizons, gaps, labels, fit/calibration boundaries and
candidate budget before empirical model selection. Keep failed experiments and
do not replace full-policy research with accumulating event-specific exceptions.
