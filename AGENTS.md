# Reversal / Mean-Reversion Strategy Bucket

Read `CONTINUE_HERE.md` and `docs/ops/evidence/r1a_cloud_raw_replay_9abe704/REPORT.md`.

## Delivered data: use the actual cloud pack

`data/r1a_carrier_prices/cloud_pack_v1/` contains 12 real CSVs (10 annual price files and 2 action files), not pointers. Cloud run 34628912555 hash/size/row-verified them without private/ or local DataHub and reproduced the 588000 coverage and return ledgers byte-for-byte.

Acceptance: `PUBLIC_CSV_DELIVERY_VERIFIED_SECONDARY_RAW_REPLAY_REPRODUCED`.
Research: `PARTIAL_CARRIER_TRANSPORT`.

Do not repeat the obsolete claim that ETF bytes are only local or send another routine data-transfer task to the user. Use the public manifest directory explicitly; the old root manifests retain private-export provenance and are not the cloud execution path.

## Scientific boundaries

588000 remains descriptive secondary transport: 1,791/1,802 pairs and seven unchanged horizons. Reproduction is not fresh independent OOS or new statistical certification.

512100 is data-delivered but outcome-blocked. 2021 positive-volume coverage is 94.478738% <95%; existing complete-path pair availability is 846/1,296 =65.2778% pooled and 4.8387% in 2021. The availability-only calculation is not a return measurement or permission to relax a gate.

Source-recorded zero volume is not independently verified exchange no-trade. The local source audit explicitly retains that distinction as unknown. Diagnose with available cloud data and legitimate evidence, not by optimizing strategy returns. A later methodological alternative needs a separate justified freeze, not a silent change to v1.

Do not change signals, pair matching, time alignment, seven horizons, side/year selection, or gate thresholds. No filling, replacement ETF or reopening closed R1_B/R2/MO identities. Preserve all previous receipts and source data. Do not choose a holding period from the observed tables.

`BLACKBOX_query_count=3`; no query #4; `production_authority=false`; `fresh_oos=false`.
